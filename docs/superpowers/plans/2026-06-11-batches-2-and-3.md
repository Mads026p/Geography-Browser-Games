# GeoSphere Batches 2 and 3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Complete the approved safety-fix and asset-cleanup batches as two separately tested and committed checkpoints.

**Architecture:** Keep the existing static application intact. Add narrowly scoped, testable support modules for server request handling and browser safety behavior, then centralize runtime asset paths without beginning the larger `app.js` decomposition reserved for Batch 6.

**Tech Stack:** Browser JavaScript, Node.js ESM, `node:test`, HTML/CSS, Git.

---

## Batch 2: Safety Fixes

### Task 1: Harden the local server

**Files:**
- Create: `tools/lib/static-server.mjs`
- Create: `tests/static-server.test.mjs`
- Modify: `tools/serve-local.mjs`

- [ ] Write request-path and HTTP behavior tests covering normal files, `HEAD`, unsupported methods, malformed escapes, hidden files, and traversal attempts.
- [ ] Run `node --test tests/static-server.test.mjs` and confirm the new module is missing.
- [ ] Extract a static-server factory with resolved-root containment, method checks, safe decoding, hidden/sensitive-file denial, security headers, and stream error handling.
- [ ] Re-run the server tests and confirm all cases pass.

### Task 2: Add safe storage and autocomplete aliases

**Files:**
- Create: `app-support.js`
- Create: `tests/app-support.test.mjs`
- Modify: `index.html`
- Modify: `app.js`

- [ ] Write tests for blocked storage, malformed JSON, typed defaults, safe writes, and common country aliases.
- [ ] Run `node --test tests/app-support.test.mjs` and confirm the support API is missing.
- [ ] Implement `GeoSphereSupport` with safe storage access and centralized alias lookup.
- [ ] Replace direct `localStorage` access in `app.js` and connect country matching to the centralized aliases.
- [ ] Re-run support tests and data validation.

### Task 3: Add startup diagnostics and clear obvious console errors

**Files:**
- Modify: `app-support.js`
- Modify: `tests/app-support.test.mjs`
- Modify: `index.html`
- Modify: `styles.css`
- Modify: `app.js`

- [ ] Add failing tests for missing or empty startup datasets.
- [ ] Implement startup diagnostics and a dismissible, nonblocking warning panel.
- [ ] Start the local server, load the app in the browser, inspect console/runtime behavior, and fix only errors directly exposed by this batch.
- [ ] Run all Batch 2 tests, syntax checks, and validation.
- [ ] Commit Batch 2 independently.

## Batch 3: Asset Cleanup

### Task 4: Audit and centralize runtime asset paths

**Files:**
- Create: `asset-paths.js`
- Create: `tests/asset-paths.test.mjs`
- Modify: `index.html`
- Modify: `app.js`
- Modify: `plane-renderer.js`
- Modify: `tools/audit-assets.mjs`
- Modify: `tools/validate-data.mjs`

- [ ] Write failing tests for canonical data, flag, vendor, image, and plane paths.
- [ ] Implement a browser-global asset manifest and use it at all runtime call sites.
- [ ] Teach validation and audit scripts to read the canonical paths.
- [ ] Re-run tests and confirm no missing runtime assets.

### Task 5: Remove only proven-unused duplicate runtime files

**Files:**
- Modify: `ASSET_PIPELINE.md`
- Delete only files satisfying all of these checks:
  - absent from runtime and tool references,
  - redundant with an actively used canonical source,
  - not documented as a source/master asset,
  - confirmed by the audit after path centralization.

- [ ] Generate and review the full audit report.
- [ ] Record every proposed deletion and its canonical replacement.
- [ ] Delete only the reviewed redundant files; retain source artwork, extra flags, and silhouettes when future or source value is plausible.
- [ ] Run all tests, syntax checks, validation, and the asset audit.
- [ ] Load the app and verify flags, globe data, time zones, and plane assets still resolve.
- [ ] Commit Batch 3 independently.
